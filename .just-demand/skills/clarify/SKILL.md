---
name: clarify
description: 将模糊需求逐步澄清为可验收目标。新需求或反馈偏差时使用。
---

一次只问一个问题。先复述当前理解，再提供 2-5 个可选答案，优先询问用户能看到的效果、交互、边界和验收方式。不要要求用户写完整设计说明，也不要在澄清完成前编辑代码。

关键问题确认后，输出 Markdown 表格、ASCII 布局图或时序图，明确包含、暂不包含和验收方式，并等待用户确认。确认后运行 `python3 .just-demand/scripts/task.py create ...`，再运行 `activate`。
